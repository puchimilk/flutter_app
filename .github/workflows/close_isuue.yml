name: テストアクション

on:
  push:
    branches:
      - 'feat/**'

env:
  BRANCH_NAME: ${{ github.head_ref || github.ref_name }}

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
      - name: echo
        run: |
            echo Checkout with $BRANCH_NAME
