name: テストアクション

on:
  push:
    branches:
      - 'feat/**'

env:
  BRANCH_NAME: ${{ github.head_ref || github.ref_name }}

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout
        uses: actions/checkout@v3
        with:
          ref: '$BRANCH_NAME'
          run: |
            echo Checkout with $BRANCH_NAME
